```java
package Unit4;

//实现从亿到零的整数向汉字转换
public class NumberToChinese {
    private String[] hanArr = {"零", "壹", "贰", "叁", "肆", "伍", "陆", "柒", "捌", "玖" };
    private String[] unitArr = {"十", "百", "千", "万", "十万", "百万", "千万", "亿" };

    /**
     * @param num 要判断位数的数字
     * @return i位数
     */
    private int geti(int num) {
        int i = 0;
        while (num > 0) {
            num = num / 10;
            i++;//num是i位数
        }
        return i;
    }


    /**
     * 
     * @param num 要找到num的第i位上的数字
     * @param i  要找到num的第i位上的数字
     * @param j  num一共有j位
     * @return  第i位上的数字
     */
    private int getNum(int num,int i,int j){
        int b=num/((j-i)*10);
        
    }
    
    /**
     * @param number 要转换的数字
     * @return 转换成的汉字
     */
    private String NuToCh(int number) {
        String result;
        int i = geti(number);
        switch (i){
            case 9:
                

        }

    }
}
```
